export {
  ballRadius,
  boardHeight,
  boardWidth,
  columns,
  paddleHeight,
  paddleWidth,
  paddleY,
  rows,
} from "./domain";
export {
  advance,
  ball,
  brickAt,
  launch,
  lives,
  newMatch,
  paddle,
  paddleDirection,
  pause,
  resume,
  score,
  situation,
} from "./view-model.ts";
